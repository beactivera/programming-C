#include<stdio.h>
 
int main()
{
    int d;             // deklaracja
    d = 10;            // inicjalizacja
    printf("%d\n", d); // odczyt wartosci
    d = d + 1;         // modyfikacja
    printf("%d\n", d);
    return 0;
}